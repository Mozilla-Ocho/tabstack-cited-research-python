# Copy to .env and `source .env`, or export in your shell. Never commit .env.
TABSTACK_API_KEY=
# Baseline (System A) only:
SEARCH_PROVIDER=serper            # serper | brave
SEARCH_API_KEY=
MODEL_BASE_URL=https://api.openai.com/v1
MODEL_API_KEY=
MODEL_ID=
